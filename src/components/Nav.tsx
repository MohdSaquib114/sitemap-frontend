export default function Nav() {
  return (
    <header className="mx-auto flex max-w-2xl items-center justify-between px-6 py-6">
      <span className="font-mono text-xs uppercase tracking-widest text-ink-muted">Sitemap / Scan</span>
      <a href="#crawl" className="font-mono text-xs text-ink-muted hover:text-ink">start →</a>
    </header>
  );
}